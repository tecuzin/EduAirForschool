#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../pdftohtmljs/bin/pdftohtmljs" "$@"
  ret=$?
else 
  node  "$basedir/../pdftohtmljs/bin/pdftohtmljs" "$@"
  ret=$?
fi
exit $ret
