#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../pdf-text-extract/bin/pdf-text-extract.js" "$@"
  ret=$?
else 
  node  "$basedir/../pdf-text-extract/bin/pdf-text-extract.js" "$@"
  ret=$?
fi
exit $ret
